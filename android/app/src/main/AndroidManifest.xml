<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Camera and Storage permissions -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!--Optional permissions-->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <!-- For devices running Android 12 (API level 32) or higher and integrating Agora Voice SDK version v4.1.0 or lower, you also need to add the following permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- For Android 12 or higher, the following permissions are also required -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chatku">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Chatku">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.chatdetail.ChatDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".presentation.dialing.DialingActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku" />

        <activity
            android:name=".presentation.incoming.IncomingCallActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <activity
            android:name=".presentation.voicecall.VoiceCallActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:screenOrientation="portrait" />

        <activity
            android:name=".presentation.creategroup.CreateGroupActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".presentation.groupchatdetail.GroupChatDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".presentation.imageviewer.ImageViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku" />

        <activity
            android:name=".presentation.videoviewer.VideoViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".presentation.mediapreview.MediaPreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.Chatku"
            android:screenOrientation="portrait" />

        <!-- FCM Service -->
        <service
            android:name=".service.ChatkuFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FileProvider for camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>